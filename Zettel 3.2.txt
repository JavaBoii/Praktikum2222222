a) x ist 11, weil der Wert, auf den p zeigt, um eins erhöht wird.

b) x bleibt 10, p zeigt nun auf einen undefinierten Speicherbereich nach x.

c) x (und y) sind 11, da y eine Referenz auf x ist und beide erhöht werden.

d) array ist unverändert, x ist 4, da x eine separate Variable ist und nicht das Array ändert.

e) array ist {1, 2, 4, 4}, da x das dritte Element des Arrays referenziert und es erhöht.

f) name ist "Xello world!", da q und p auf das erste Element zeigen und es zu 'X' ändern.